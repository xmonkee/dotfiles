[user]
    name = Mayank Mandava
    email = mayank@benchling.com
[core]
    editor = vim
[alias]
    co = checkout
    d = diff
    rd = "!sh -c 'git fetch; git rebase origin/dev'"
    ri = rebase -i origin/dev
    ra = rebase --continue
    ca = commit --amend --no-edit
    caa = "!sh -c 'git add . && git commit --amend --no-edit'"
    l = log --stat -C --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %Cgreen(%cr) %C(bold blue)<%an>%Creset\n\n  %C(bold cyan)%s%Creset\n'
    lf = log --stat -C --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %Cgreen(%cr) %C(bold blue)<%an>%Creset\n\n  %C(bold cyan)%s\n\n%b%Creset\n'
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
    ld = "!sh -c 'git lg --left-right --cherry-pick HEAD...origin/dev'"
    lu = "!sh -c 'git lg --left-right @{u}...HEAD'"
    lr = "!f() { git lg --cherry-pick --left-right origin/dev.. \"$1\"...HEAD; }; f"
    s = "!sh -c 'git l -1 && echo && git status -sb && echo'"
    ad = "!arc diff HEAD^"
    am = "!arc amend"
;[sequence]
    ;editor = rebase-editor
[gui]
    recentrepo = /Users/mayank/aurelia
[guitool "Rebase interactive"]
    cmd = EDITOR=code git rebase -i $REVISION
    revprompt = yes
[pager]
       diff = diff-so-fancy | less --tabs=1,5 -RFX
       show = diff-so-fancy | less --tabs=1,5 -RFX
